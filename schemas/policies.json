{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "policies.json",
  "title": "Policy Extension",
  "description": "Allows to extend TMForum Entities with odrl:policies.",
  "type": "object",
  "properties": {
    "policy": {
      "type": "array",
      "description": "A list of odrl:policy that should be connected to the entity.",
      "items": {
        "$ref": "./policy.json"
      }
    }
  },
  "required": ["policy"]
}