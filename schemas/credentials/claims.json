{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "claims.json",
  "title": "Claims Value",
  "description": "Specifies the restrictions to be configured for claims of a value.",
  "type": "object",
  "properties": {
    "name": {
      "description": "Name of the claim to be configured. If no path is configured, allowedValues will be applied to the claim with that concrete name.",
      "type": "string",
      "examples": ["role"]
    },
    "path": {
      "description": "Json Path to locate the claim for appying the allowedValues.",
      "type": "string",
      "examples": ["$.role",  "$.roles[?(@.target==\\\"my-service-id\\\")].names[*]"]
    },
    "allowedValues": {
      "description": "The values that are allowed for the defined claim",
      "type": "array",
      "items": {
        "type": "object"
      },
      "examples": [["OPERATOR","READER"], [{"role": "READER"}]]
    }
  },
  "required": ["allowedValues", "name"]
}