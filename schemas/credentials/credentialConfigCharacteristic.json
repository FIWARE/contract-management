{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "credentialConfigCharacteristic.json",
  "title": "Credential Configuration Characteristic",
  "description": "Specifies a Characteristic Value to contain a credentials configuration.",
  "type": "object",
  "properties": {
    "valueType": {
      "description": "The value type of a Credential Config Characteristic needs to be `credentialsConfiguration`",
      "const": "credentialsConfiguration"
    },
    "productSpecCharacteristicValue": {
      "description": "Only values of type credentialsConfiguration are allowed",
      "type" : "array",
      "items": {
        "$ref": "./credentialConfigValue.json"
      }
    }
  },
  "required": ["valueType", "productSpecCharacteristicValue"]
}