{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "constraint.json",
  "title": "Schema of an odrl:constraint",
  "description": "Definition of an ODRL Constraint, see https://w3c.github.io/poe/vocab/#term-Constraint.",
  "type": "object",
  "properties": {
    "@type": {
      "type": "string",
      "description": "Type of the constraint."
    },
    "@id": {
      "type": "string",
      "description": "Id of the constraint."
    },
    "odrl:operator": {
      "type": ["object","string"],
      "description": "The operator function applied to operands of a Constraint."
    },
    "odrl:leftOperand": {
      "type": ["object","string"],
      "description": "The left operand in a constraint expression."
    },
    "odrl:rightOperand": {
      "type": ["object","string"],
      "description": "Right operand for constraint expression."
    }
  },
  "required": ["odrl:operator", "odrl:leftOperand", "odrl:rightOperand"]
}